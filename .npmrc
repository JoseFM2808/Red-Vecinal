# @privy-io/react-auth trae un peer OPCIONAL (permissionless, smart wallets que no usamos)
# que pide ox@^0.8 mientras viem 2.55 usa ox@0.14. El peer es opcional y no se instala,
# asi que la resolucion legacy es segura aqui. Ver ADR-050.
legacy-peer-deps=true
